/* pages/detail/detail.wxss */

.detail-wrapper {
  position: relative;
}

.movie-image {
  position: absolute;
  width: 204rpx;
  height: 300rpx;
  top: 30rpx;
  left: 273rpx;
  z-index: 99;
}

.movie-wrapper {
  position: absolute;
  top: 60rpx;
  left: 50rpx;
  right: 50rpx;
  margin-bottom: 50rpx;
  display: flex;
  flex-flow: column;
  align-items: center;
  background: white;
  border: solid 1rpx #eaeef1;
  border-radius: 10rpx;
}

.movie-title {
  padding-top: 295rpx;
  font-size: 36rpx;
  line-height: 50rpx;
  color: #333;
  font-weight: 70;
}

.movie-desc {
  font-size: 26rpx;
  line-height: 40rpx;
  color: #666;
  margin: 20rpx 60rpx;
}

/* 以下为底部弹窗样式 */

.detail-wrapper {
  position: relative;
}

.movie-image {
  position: absolute;
  width: 204rpx;
  height: 300rpx;
  top: 30rpx;
  left: 273rpx;
  z-index: 99;
}

.movie-wrapper {
  position: absolute;
  top: 60rpx;
  left: 50rpx;
  right: 50rpx;
  margin-bottom: 50rpx;
  display: flex;
  flex-flow: column;
  align-items: center;
  background: white;
  border: solid 1rpx #eaeef1;
  border-radius: 10rpx;
}

.movie-title {
  padding-top: 295rpx;
  font-size: 36rpx;
  line-height: 50rpx;
  color: #333;
  font-weight: 70;
}

.movie-desc {
  font-size: 26rpx;
  line-height: 40rpx;
  color: #666;
  margin: 20rpx 60rpx;
}

/* 以下为底部弹窗样式 */

/* mask */
.pop_screen {
  width: 100%;
  height: 100%;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;
  background: #000;
  opacity: 0.2;
  overflow: hidden;
}

/* content */
.pop_box {
  width: 100%;
  overflow: hidden;
  position: fixed;
  bottom: 0;
  left: 0;
  z-index: 1001;
  background: #fff;
}

.pop_content {
  overflow-y: scroll;
}

.pop_title {
  padding: 30rpx;
  font: 36rpx;
  text-align: center;
}

.line {
  border-bottom: 1px solid #edecef;
}

.line2 {
  background: #edecef;
  height: 10rpx;
}
